# Data-Pipeline-CSV-to-Postgre-SQL
Flow: CSV (raw) → Spark (clean &amp; transform) → PostgreSQL (employees_clean)  Tech Stack  Docker &amp; Docker Compose  Apache Spark (PySpark or Scala)  PostgreSQL  JDBC for Spark ↔ Postgres
